; OpsPilot PHP overrides (applied to both php-fpm and php-cli).
; PHPStan/Larastan at level 5 needs more than the stock 128M when the result
; cache is cold; composer and some artisan commands benefit too.
memory_limit = 512M
